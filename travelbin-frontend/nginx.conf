server {
    listen 3001;

    root /usr/share/nginx/html;
    index index.html;

    # Hash routing — browser only ever requests / and asset files.
    # try_files still needed to serve index.html for any non-asset path.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
